apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-b
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-b
  template:
    metadata:
      labels:
        app: service-b
    spec:
      containers:
        - name: service-b
          image: service-b:1.0.0
          ports:
            - containerPort: 9200
        - name: proxy-b
          image: service-mesh-proxy:1.0.0
          ports:
            - containerPort: 8080
          env:
            - name: LOCAL_SERVICE_NAME
              value: "service-b"
            - name: LOCAL_SERVICE_PORT
              value: "9200"
---
apiVersion: v1
kind: Service
metadata:
  name: service-b
spec:
  selector:
    app: service-b
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
